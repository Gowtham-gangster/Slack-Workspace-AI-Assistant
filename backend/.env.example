PORT=3001
NODE_ENV=development

# JWT Secret — use a long, random string (at least 64 characters) in production
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET

# Allowed CORS origins (comma-separated) — only needed in production
# Example: ALLOWED_ORIGINS=https://your-app.com,https://www.your-app.com
ALLOWED_ORIGINS=

# MySQL Database Configuration
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=YOUR_DB_PASSWORD
MYSQL_DATABASE=slack_ai_assistant

# Google OAuth (for Google Sign-In)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# Slack App OAuth 2.0 Credentials
SLACK_CLIENT_ID=11413856018628.11408171816918
SLACK_CLIENT_SECRET=aec47de704943e0d36ac65055090e660
SLACK_REDIRECT_URI=http://localhost:3001/api/auth/slack/callback
FRONTEND_SETTINGS_URL=http://localhost:7505/settings

